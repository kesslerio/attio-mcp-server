name: Attio MCP
description: Connect Attio CRM to MCP clients
keywords: [attio, crm, contacts, companies, deals]

startCommand:
  type: http
  runtime: typescript

# Keep UI config simple at publish time; your TS entrypoint already exports configSchema
# Optionally: help users with a sample (doesn't mark fields as "required")
exampleConfig:
  ATTIO_API_KEY: "<paste-your-attio-api-key>"
  ATTIO_WORKSPACE_ID: "<optional>"
  debug: false