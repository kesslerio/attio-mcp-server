# MCP Server Requirements for Codex Integration
# This file contains Python dependencies for custom MCP servers or development

# Core MCP SDK for Python (if developing custom servers)
mcp>=1.0.0

# HTTP client for API interactions
httpx>=0.25.0

# Data validation
pydantic>=2.0.0

# Development server dependencies (for custom servers)
uvicorn>=0.24.0
fastapi>=0.104.0

# Utility packages
python-dotenv>=1.0.0
click>=8.0.0
rich>=13.0.0

# Note: Brave Search MCP Server is installed via NPX, not pip
# Run: npx -y @modelcontextprotocol/server-brave-search
# For Docker: docker run -i --rm -e BRAVE_API_KEY mcp/brave-search